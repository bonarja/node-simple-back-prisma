generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider = "mysql"
  url      = env("DATABASE_URL")
}

model notes {
  id          Int      @id @default(autoincrement())
  title       String   @db.Text
  description String   @db.Text
  createAt    DateTime @db.Date
  user_id     Int?
  users       users?   @relation(fields: [user_id], references: [id], onDelete: NoAction, onUpdate: NoAction, map: "FK_notes_users")

  @@index([user_id], map: "FK_notes_users")
}

model users {
  id       Int     @id @default(autoincrement())
  name     String  @default("0") @db.VarChar(50)
  password String  @default("0") @db.VarChar(100)
  email    String? @db.VarChar(50)
  role     String? @default("user") @db.VarChar(50)
  notes    notes[]
}
